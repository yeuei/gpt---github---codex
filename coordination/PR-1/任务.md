# [~] PR #1 总任务：完成本地 GitHub ↔ ChatGPT Web 触发器 V1 的审批式模拟交接，并记录可复现运行证据

> 本文件是累积任务合同：只增不减。
> ChatGPT 创建/追加任务；Agent 只根据真实执行状态修改状态标记。

## 状态

```text
[ ] TODO
[~] RUNNING
[x] DONE
[!] BLOCKED
[?] WAITING_USER
[-] SUPERSEDED
```

## 子任务

- [x] T1.1 核对并固定当前 V1 触发器实现与安全/路由契约，确保实现与 `docs/技术规范.md` 一致。
- [x] T1.2 使用真实 PR #1 建立并接管 `coordination/PR-1/` 三文件交接；Agent 拉取后核验并更新真实状态。
- [x] T1.3 完成 Agent → ChatGPT Web 审批式事件的真实链路证据，关联 bootstrap 事件 `ggc-pr-bootstrap-001`。
- [x] T1.4 用户已在 gitignored 的本机配置中明确设置 `agent.command`；反向事件 `ggc-pr-1-chatgpt-command-decision-001` 已按自动审批规则启动本地 Agent 进程。
- [x] T1.5 核对去重、断线、假成功、权限与回环处理，将当前有效结论维护在 `docs/触发器问题与解决方案.md`。
- [~] T1.6 汇总 GitHub HEAD、PR 三文件、本地 SQLite 时间线与运行证据，满足完整闭环验收后再关闭总任务。

## 依赖关系

- T1.2 是后续正式 PR 交接的基础。
- T1.3、T1.5 在不破坏当前运行的前提下可并行推进。
- T1.4 依赖 T1.2，且已取得用户明确配置的 `agent.command` 与实际启动证据。
- T1.6 依赖 T1.3、T1.4、T1.5 的真实证据。

## 当前范围边界

本 PR 只服务一个可关闭目标：完成 Local Trigger V1 的审批式双向模拟交接与可复现证据。

明确不在本 PR 扩展：

- 新的独立产品目标；
- 让 LLM 自主决定网页点击路径；
- 保存或读取浏览器 cookie、密码、token 等秘密；
- 在未配置 `agent.command` 时猜测本地启动命令。

## 验收条件

总任务只有在以下条件同时成立时才能标 `[x]`：

- 所有仍有效子任务已 `[x]`；
- 不存在 `[!] BLOCKED`；
- 不存在 `[?] WAITING_USER`；
- Agent → ChatGPT Web 与 ChatGPT → Agent 均有真实事件/运行证据；
- GitHub 当前 HEAD、PR 三文件与 SQLite 时间线互相一致；
- 没有重复事件、虚构 PR 号或把草稿填入/单元测试冒充完整闭环。
