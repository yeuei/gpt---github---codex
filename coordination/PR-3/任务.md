# [x] PR #3 总任务：按四个基础仓库最新协议完成 Local-first Dashboard 重构

> 本文件是累积任务合同：只增不减。
> ChatGPT 创建/追加任务；状态只依据真实代码、GitHub 与测试事实更新。

## 状态

```text
[ ] TODO
[~] RUNNING
[x] DONE
[!] BLOCKED
[?] WAITING_USER
[-] SUPERSEDED
```

## 子任务

- [x] T3.1 完整读取四个基础仓库最新协议，以及当前实例 README、docs、coordination、Trigger 代码和完整 Git 提交历史。
- [x] T3.2 将 Dashboard 默认数据源改为本地 Git clone/history；后台扫描不执行网络 fetch，仅保留显式“从 GitHub 刷新”入口。
- [x] T3.3 每个事件节点严格按自身 commit SHA 读取 `coordination/PR-N/任务.md`；不存在时显示历史快照不可用，不按时间或当前任务回退。
- [x] T3.4 将 Trigger 审批状态、投递/执行状态、GitHub PR 状态拆成独立状态域；旧混合记录无法拆分时显示 `legacy_unknown`。
- [x] T3.5 删除旧多层 renderer 运行路径，统一为 `trigger/dashboard.html` 单 renderer，统一筛选、节点、拖动/缩放、点击和任务详情逻辑。
- [x] T3.6 README 保留协议区并维护当前项目事实；同步 docs、coordination v3、Trigger/发布说明和可复现 CI。
- [x] T3.7 修复 GitHub Actions 动态 import 的 `sys.modules` 注册问题，并确认 workflow run `34077319339` 在 commit `e411794fe79a322bb39cfd90fe0442c880180602` 上真实 `success`。
- [x] T3.8 核对 PR #3 最终 diff、真实 PR 状态和已合并 PR #1 coordination 清理；不改写 Git 历史，不把 PR #2 未合并内容当作本次事实真源。

## 依赖关系

- T3.2–T3.6 建立在 T3.1 的协议/历史核验之上。
- T3.7 依赖实现提交后 GitHub Actions 的真实运行结果。
- T3.8 是文档与 PR 收口，不要求伪造本机 Chrome/Open Browser Use/Codex app-server 运行证据。

## 已验证事实

- 真实 PR：`#3`
- branch：`refactor/local-git-dashboard`
- base：`main`
- PR #1：已合并，历史保留；其 coordination 工作目录从本 PR HEAD 清理。
- PR #2：真实开放、独立工作；未被合并进本 PR。
- 主体重构 commit：`baa15b2678454c0a75fecd0e6d670a945c60cf2f`
- CI 修复 commit：`e411794fe79a322bb39cfd90fe0442c880180602`
- GitHub Actions run：`34077319339`，结论 `success`。

## 边界

以下内容没有被宣称为已完成：

- 用户本机真实 Chrome / Open Browser Use 的重新运行；
- 用户本机 Codex app-server 的重新运行；
- PR #2 的合并或关闭；
- PR #3 的 merge。

这些都不能由远程 CI 或 ChatGPT 猜测。

## 验收条件

- 所有本 PR 约定的代码/协议重构任务已完成；
- Python compile、unit tests、Dashboard JavaScript syntax、Git whitespace check 可由 GitHub Actions 复现；
- 历史任务只按 exact commit SHA；
- 后台不自动联网；
- 三类状态不互相推断；
- 当前 README/docs 使用真实 PR #3 事实；
- 未虚构本机运行、PR 状态或历史。
