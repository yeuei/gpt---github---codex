# 项目总览

> 本文件描述当前项目“是什么、为什么做、有哪些阶段、当前主线是什么”。
> 只保留当前有效版本；历史通过 Git 查看。

## 1. 项目名称

`gpt---github---codex`

## 2. 项目目标

本仓库服务于一个本地常驻的 **GitHub ↔ ChatGPT Web ↔ Codex** 触发器。

已冻结目标：

- 监听实际交接仓库的 GitHub 分支更新，并以 Git trailer 的 origin/event id 路由事件；
- 从 Agent 更新唤醒固定的 remote ChatGPT Web Chat；从 ChatGPT 更新唤醒本地 Agent；
- 提供本地 Dashboard，支持全局暂停、两个方向独立开关、逐条人工审批及按 PR 的交接时间线；
- 第一版浏览器层使用 `open-browser-use`，但由确定性程序调用 CLI，不由 LLM 决定网页操作；
- 用本仓库进行真实分支/PR 的模拟交接，并据失败和回环改进三份基础协议仓库。

## 3. 当前主线

```text
Agent-origin commit
  ↓
本机 Trigger：解析 trailer、去重、记录
  ↓（人工批准，可单方向关闭）
ChatGPT Web 固定对话：重新读取 GitHub
  ↓
ChatGPT-origin commit
  ↓（人工批准，可单方向关闭）
本地 Codex Agent：fetch 后继续
```

## 4. 当前阶段

- 当前阶段：`V1 本地 Trigger 实现；先建立真实 PR，再完成审批式双向模拟交接`
- 当前主要仓库：`yeuei/gpt---github---codex`
- 当前关键 PR：`无`

## 5. 当前不变量

- GitHub 当前 HEAD 是项目事实真源；
- `任务.md`、`agent汇报.md`、`chatgpt解惑.md` 三文件职责分离；
- 不预建虚构 PR 目录；
- 不创建版本号文档保存历史；
- 普通工程问题由 Agent 自行处理；
- 规划问题由 ChatGPT 处理；
- 改变核心项目口径的问题由用户决定。

## 6. 当前未决项

- Chrome extension 已安装，弹窗曾显示 native host connected；但 CLI relay socket 可能在 broker 结束后失效，必须以 Dashboard 的 CLI 连接检测结果为准。
- 当前真实浏览器草稿曾成功出现在目标 ChatGPT 对话，但尚未由用户手动发送；因此远程 ChatGPT 侧尚无本次模拟的新回复证据。
- `agent.command` 必须由用户在本机 `trigger/config.local.json` 中明确配置；系统绝不猜测或自动执行命令。
- 当前还没有真实任务 PR；创建后才可开始 end-to-end 模拟。

## 7. 阅读路径

第一次进入项目：

1. 本文件；
2. `docs/技术规范.md`；
3. 当前 PR 的 `coordination/PR-<N>/任务.md`（若存在）；
4. 如果是 ChatGPT，再读 `agent汇报.md`；
5. 如果是 Agent，再读 `chatgpt解惑.md`。
